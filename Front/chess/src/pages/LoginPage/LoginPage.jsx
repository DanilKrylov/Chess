import React from 'react';
import MainWrapper from '../../components/MainWrapper/MainWrapper';
import LoginForm from '../../modules/Authorize/LoginForm';

const LoginPage = () => {
    return (
        <MainWrapper>
            <LoginForm></LoginForm>
        </MainWrapper>
    );
};

export default LoginPage;